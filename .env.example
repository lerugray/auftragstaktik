# Ship tracking — free API key at aisstream.io (sign in with GitHub)
AISSTREAM_API_KEY=your-key-here

# Briefing generator — defaults to Ollama (free, local)
# Only change these if you know what you're doing
# LLM_PROVIDER=ollama
# LLM_MODEL=llama3
# LLM_BASE_URL=http://localhost:11434/v1
